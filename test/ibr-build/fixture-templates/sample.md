# Sample template

A minimal markdown fixture used by the `/ibr:build` smoke test. Not consumed
by users — only by `indexTemplates()` to confirm the central-dir scan picks
up at least one `.md` file. Needs to remain checked in (do not gitignore).
